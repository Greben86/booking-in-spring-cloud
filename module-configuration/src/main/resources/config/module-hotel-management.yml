spring:
  application:
    name: module-hotel-management
  r2dbc:
    url: r2dbc:h2:mem:///mydb
    username: sa
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: on
    show-sql: on
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
